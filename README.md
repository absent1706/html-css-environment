# Gulp ready-to-go environment

Features:

 * less compile
 * watch
 * notifications
 * browsersync

# Installation

```
npm install gulp-cli -g
npm install
gulp serve
```

visit now [http://localhost:300/](http://localhost:300/)
